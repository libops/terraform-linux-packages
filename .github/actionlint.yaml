paths:
  .github/workflows/reusable-goreleaser.yaml:
    ignore:
      # GitHub Cloud exposes this reusable-workflow identity context, but
      # actionlint v1.7.12 has not added it to the job context model yet.
      - 'property "workflow_repository" is not defined in object type'
      - 'property "workflow_sha" is not defined in object type'
