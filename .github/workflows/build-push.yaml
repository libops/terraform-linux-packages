name: build-push

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - Dockerfile
      - .github/workflows/build-push.yaml

jobs:
  run:
    uses: libops/.github/.github/workflows/build-push.yaml@main
    permissions:
      contents: read
      packages: write
    secrets: inherit
